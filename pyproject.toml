[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "checkai" 
version = "0.1.0"
description = "结合体检报告扫描并结构化、RAG、个人画像的ai系统"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["backend", "frontend"]
